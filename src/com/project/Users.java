package com.project;

public class Users {
    String username;
    String password;


    public static boolean signIn() {

        System.out.println("If you want to go back to main menu, type 0 in both username and password.");
        /*

        if(code to check if username and password match)
            return true;
        else if (Code when username is correct and password is not )
        {
            System.out.println("Incorrect Password.");
            return false;
        }
        else if (Code when username is not found)
        {
            System.out.println("Username not found.");
            return false;
        }
            */
        return false;
    }
}
